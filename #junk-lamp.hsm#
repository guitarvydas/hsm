machine Lamp {
  state OFF {
    on pwr { next ON }
  }
  state ON {
    enter { ⟨print ('enter ON')⟩ }
    on pwr {
      send power ⟨True⟩
      next OFF
    }
   delegate Brightness
   enter { ⟨print ('exit ON')⟩ }
  }
}
